import {environment} from "../../../../environments/environment";

export class BaseService {
    protected securityBaseUrl = environment.securityBaseUrlV1;
    // protected baseUrl = environment.baseUrlV1;
}
